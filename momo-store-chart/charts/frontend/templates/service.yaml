apiVersion: v1
kind: Service
metadata:
  name: frontend
  namespace: momo-store
  labels:
    app: frontend
spec:
  type: ClusterIP
  ports:
    - port: {{ .Values.serviceContainerPort }}
      protocol: TCP
      targetPort: {{ .Values.serviceContainerTargerPort }}
  selector:
    app: frontend
